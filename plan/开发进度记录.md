# 开发进度记录

更新时间：2026-09-22

## 当前状态

项目可演示基础 MVP 已完成。账号—小组—权限—任务—成果—文件构成主业务链，临时取件、个人跨设备空间和通知中心构成配套工具链。AI 协作 Agent 已完成方向确认并正式纳入下一增强阶段，首个模型供应商确定为 DeepSeek API。

## 已完成

1. 建立前后端分离目录：`frontend/`、`backend/`、`docs/development/`。
2. 前端采用 Vue 3、TypeScript、Vite、Pinia、Vue Router、Element Plus。
3. 完成前端主布局、侧边导航和工作台首页，小组、任务、文件、临时中转、设备、通知均已有可操作页面。
4. 建立 Axios 请求层、系统健康检查接口和基础会话状态仓库。
5. 后端采用 Spring Boot 3.5.6、Java 21、Spring Security、JPA、Flyway。
6. 按业务分区建立 `user`、`group`、`permission`、`task`、`collaboration`、`file`、`transfer`、`device`、`workbench`、`audit` 包。
7. 建立统一响应、分页响应、业务错误码、业务异常和全局异常处理。
8. 建立安全过滤器基线：健康检查公开，其他业务接口默认要求认证。
9. 配置 H2 开发数据库和 MySQL 运行 Profile。
10. 完成第一版 Flyway 迁移，包含用户、小组、成员关系和权限授权四张基础表。
11. 加入 Maven Wrapper，本机不安装 Maven 也可运行后端。
12. 更新根目录 README、本地环境变量示例、Git 忽略规则和工程协作约定。
13. 完成账号注册、登录、退出、当前用户查询和 JWT 无状态认证。
14. 完成小组创建、我的小组列表、成员列表和成员关系校验。
15. 完成限时限次邀请码、邀请码加入小组以及并发使用锁定。
16. 完成组长为成员授予或撤销五类权限的接口与页面。
17. 完成登录注册页、小组管理页、会话持久化和前端路由守卫。
18. 完成任务发布、多人指派、优先级、截止时间和成员开始任务。
19. 完成成果分版本提交、附件链接预留和最新版本展示。
20. 完成任务审核通过、退回修改、再次提交和最终验收状态流转。
21. 完成组长或具有 `TASK_CREATE`、`TASK_REVIEW` 权限成员的服务端鉴权。
22. 完成任务工作台页面和 Flyway V3 任务协作数据表。
23. 完成本地文件存储适配层，保留切换 OSS、COS 或 MinIO 的扩展边界。
24. 完成共享资料库上传、列表、搜索、鉴权下载、重命名和软删除。
25. 完成文件大小、扩展名、物理路径和小组归属安全校验。
26. 完成任务成果引用资料库文件，替代原有外部附件链接。
27. 完成共享资料库页面、Flyway V4 文件元数据表和存储设计文档。
28. 完成临时文件中转创建、所有者历史、主动取消、有效期和下载次数限制。
29. 使用 10 位随机取件码并仅保存 SHA-256 摘要，提供无需登录且不泄露发送者信息的独立取件页。
30. 完成定时过期清理，失效文件删除后保留状态记录。
31. 完成个人跨设备文字、链接、文件的新增、查看、复制、下载和删除，并按账号强制隔离。
32. 完成站内通知列表、未读数、单条已读、全部已读和事件去重。
33. 任务指派和每次成果审核结果已自动产生通知，并可跳转至任务页面。
34. 完成 Flyway V5，新增临时中转、设备内容、通知三组数据表。
35. 完成三个工具模块前端页面，移除第一版核心菜单中的占位页面。
36. 确定 AI 协作 Agent 的产品定位、首批能力、权限确认等级和隐私同意规则。
37. 确定通过可替换适配器接入 DeepSeek API，模型密钥仅由后端安全配置管理。
38. 建立 Agent 专项开发计划，包含工具白名单、数据和接口设计、A0～A5 开发阶段、资源与安全测试要求。
39. 将 Agent 同步纳入项目目标、总体架构、业务分区、开发顺序、资源申请、风险和验收计划。

## 已验证

- `frontend` 执行 `npm run build` 成功。
- `backend` 执行 `.\mvnw.cmd test` 成功。
- 后端共 6 个测试通过，失败 0、错误 0。
- 端到端测试覆盖注册、认证、小组创建、邀请加入、权限授予、真实文件上传与下载、重命名、任务附件引用、两版成果提交、最终审核通过和通知联动。
- 工具模块测试覆盖匿名取件、下载次数耗尽、跨设备内容创建以及账号间数据隔离。
- Flyway V1 至 V5 迁移已在 H2 MySQL 兼容模式中成功执行。
- 前端构建存在单个产物体积较大的警告，不影响当前基线，后续可用路由懒加载和分包优化。

## 后续增强建议

第一版核心范围已经完成，后续按风险和答辩需要选择性开发：

1. 进入 Agent A0：建立模型适配接口、DeepSeek 实现、后端密钥配置、调用统计和模型总开关。
2. 进入 Agent A1：完成对话入口及小组、成员、任务、通知和文件元数据只读工具。
3. 进入 Agent A2：完成任务草稿、计划确认、批量发布和临时文件中转工具。
4. 在讨论功能具备数据来源后完成聊天总结；随后实现授权资料解析、检索和带来源问答。
5. 同步补齐成员移除、组长转让、邀请码管理、审计和 MySQL 实机测试等基础增强项。

“生成取件码并在未登录设备下载临时文件”“同一账号跨设备传递内容”“任务事件产生站内通知”三个阶段目标均已达到。当前适合进入课程演示、用例整理和第二轮缺陷修复。

AI 阶段的目标和完成标准以 `plan/AI协作Agent设计与开发计划.md` 为准。尚未取得 DeepSeek API 密钥前可以先完成数据表、模型适配接口、工具契约、Mock Provider、权限和确认流程，不应把密钥写入仓库。

## 重新启动命令

后端：

```powershell
cd C:\Users\ASUS\Desktop\实战问题求解\backend
.\mvnw.cmd spring-boot:run
```

前端：

```powershell
cd C:\Users\ASUS\Desktop\实战问题求解\frontend
npm run dev
```

验证：

```powershell
cd C:\Users\ASUS\Desktop\实战问题求解\backend
.\mvnw.cmd test

cd ..\frontend
npm run build
```

## 工作区注意事项

- 本次没有提交 Git，所有框架文件仍保留在当前工作区中。
- `需求分析_word/小组协作工具箱系统需求规格说明书_第4小组.docx` 当前也有未提交修改；它属于已有文档，不要在后续编码整理时覆盖或回退。
- 已停止本次启动的后端进程，可以安全关机。
